# Trained model

사용자가 학습하고 클래스 이름을 `chanyong`으로 변경한 모델을 이 폴더에 넣습니다.

```text
weights/best_chanyong.pt
```

기존 작업 폴더에서 복사하는 예시:

```bash
cp ~/Downloads/H13_smartcar_dataset_labeling_ready_v2/runs/detect/h13_detector/weights/best_chanyong.pt weights/
```
